services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: tierhub
      POSTGRES_USER: tierhub
      POSTGRES_PASSWORD: tierhub
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
