{
  "name": "@tierhub/backend",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "check": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5",
    "better-auth": "^1.5.6",
    "drizzle-orm": "^0.40",
    "hono": "^4",
    "hono-openapi": "^0.4",
    "postgres": "^3",
    "zod": "^3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31",
    "typescript": "^5.9.3"
  }
}
